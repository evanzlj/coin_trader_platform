# Producer deps (China Windows side: monitor / live_openclaw / signal_pusher / watchdog)
# Executor (btc-ml / Singapore) deps: see requirements-executor.txt
#
# Note: playwright needs only the Python package -- live_openclaw connects to an
#       already-installed browser (openclaw) over CDP; no `playwright install chromium`.

pandas>=2.0
numpy>=1.24
pyarrow>=14.0        # buffer state parquet (signal_generator.save/load_buffer_state, F6)
matplotlib>=3.7      # draw_kline signal chart rendering
playwright>=1.40     # live_openclaw (connects to existing browser via CDP)
requests>=2.31       # feishu alerts (live/notify.py; degrades silently if missing)
aiohttp>=3.9         # realtime_data_pull (RealtimeFeed WS, monitor fallback path)
websockets>=12.0     # realtime_data_pull (Binance Futures WS)

# Optional: watchdog Windows desktop toast; degrades silently via try/except if absent
plyer>=2.1
