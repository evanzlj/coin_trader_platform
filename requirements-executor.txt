# Executor deps (deployed on btc-ml / Singapore)
# China Windows monitor/openclaw deps: see requirements.txt (playwright etc.)
binance-futures-connector>=4.0.0
python-okx>=0.3.0
pandas>=2.0.0
requests>=2.31.0
